<% provide(:title, ' (I add the title - Kenny)') %> 
<% provide(:title, ' (I add the title - Yulan)') %>
//add kenny to the header

<!DOCTYPE html>
<html>
<head>
  <title><%= full_fill(yield(:title)) %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= render 'layouts/shim' %>

    
</head>
<body>
	<%= render 'layouts/header' %> <!-- the header contents can be defined for partial in _header.html.erb file -->
	<div class="container">
		<%= yield %>
		<%= render 'layouts/footer' %>
	</div>
</body>
</html>
